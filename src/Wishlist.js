import React from 'react'
import { Button } from 'reactstrap'

function Wishlist() {
  return (
    <div>
        <Button>
            Add a new wishlist
        </Button>
    </div>
  )
}

export default Wishlist